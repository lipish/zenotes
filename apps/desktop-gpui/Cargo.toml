[package]
name = "desktop-gpui"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# gpui and zed_ui are evolving fast; use git deps
# If build fails, we can pin to specific commits.
zed_ui = { git = "https://github.com/zed-ui/zed-ui" }
gpui = { git = "https://github.com/zed-ui/gpui" }

[build-dependencies]

[features]
# placeholder

