# MyNotes 使用说明

## 快速开始

### 1. 启动应用
```bash
cd mynotes
npm install
npm run dev
```
访问 http://localhost:3033

### 2. 导入 Markdown 文件

#### 方法一：从目录批量导入
1. 点击左侧边栏的 **"导入"** 按钮
2. 默认源目录为 `/Users/xinference/Sync/md`
3. 点击 **"扫描目录"** 查看所有 Markdown 文件
4. 点击 **"开始导入"** 批量导入文件

**自动处理**：
- ✅ 自动扫描所有子目录
- ✅ 自动查找并复制图片（从 `/Users/xinference/Sync/md/pics` 等位置）
- ✅ 自动更新图片链接为可访问路径
- ✅ 所有图片保存到 `public/images` 目录

#### 方法二：手动创建笔记
1. 点击 **"新建笔记"** 按钮
2. 在编辑器中直接编写内容
3. 支持富文本编辑和 Markdown 语法

### 3. 图片管理

#### 添加图片到笔记
- **拖拽**：直接拖拽图片文件到编辑器
- **粘贴**：复制图片后在编辑器中粘贴（Ctrl/Cmd+V）
- **上传**：点击工具栏的上传按钮选择图片
- **URL**：点击图片按钮输入图片 URL
- **Markdown**：使用 `![alt](url)` 语法

#### 图片管理器
1. 点击工具栏的 **图片图标** 打开图片管理器
2. 功能：
   - 查看所有已导入的图片
   - 同步本地图片目录
   - 复制图片 URL 或 Markdown 格式
   - 删除不需要的图片

### 4. 设置配置

点击工具栏的 **设置图标** 进入设置页面：

#### Markdown 设置
- **源文件目录**：设置默认导入目录（默认：`/Users/xinference/Sync/md`）
- **递归导入**：是否包含子目录
- **自动处理图片**：导入时自动复制和转换图片

#### 图片设置
- **保存目录**：图片存储位置（默认：`public/images`）
- **基础 URL**：图片访问路径前缀（默认：`/images`）
- **图片质量**：JPEG 压缩质量
- **文件命名**：使用原始名称或生成唯一名称

#### 编辑器设置
- **自动保存**：开启/关闭自动保存
- **保存间隔**：自动保存的时间间隔
- **字体大小**：调整编辑器字体大小
- **主题**：浅色/深色/跟随系统

### 5. 常用操作

#### 编辑笔记
- 点击笔记列表中的笔记进行编辑
- 工具栏提供格式化选项（加粗、斜体、标题等）
- 支持预览/编辑模式切换

#### 搜索笔记
- 使用顶部搜索框快速查找笔记
- 支持按标题、内容、标签搜索

#### 导出笔记
- 点击下载按钮导出当前笔记
- 支持 Markdown 格式导出

## 问题解决

### 图片无法显示
1. 检查图片路径是否正确
2. 使用图片管理器重新导入图片
3. 确保 `public/images` 目录有写入权限

### 导入失败
1. 检查源目录路径是否正确
2. 确保有足够的存储空间
3. 查看控制台错误信息

### 存储空间不足
1. 使用图片管理器删除不需要的图片
2. 清理旧笔记
3. 检查浏览器存储限制

## 文件结构

```
mynotes/
├── public/
│   └── images/          # 所有图片存储位置
├── app/
│   ├── api/            # API 端点
│   └── page.tsx        # 主页面
├── components/         # UI 组件
├── lib/               # 工具库
└── types/             # 类型定义
```

## 技术特性

- **本地存储**：使用 localStorage 保存笔记
- **自动保存**：2 秒自动保存更改
- **图片处理**：自动复制和管理图片文件
- **Markdown 支持**：完整的 Markdown 语法支持
- **响应式设计**：适配各种屏幕尺寸

## 快捷键

- `Ctrl/Cmd + S`：保存笔记
- `Ctrl/Cmd + B`：加粗
- `Ctrl/Cmd + I`：斜体
- `Ctrl/Cmd + U`：下划线
- `Ctrl/Cmd + V`：粘贴（支持图片）

## 注意事项

1. **数据备份**：定期导出重要笔记
2. **图片大小**：建议单张图片不超过 5MB
3. **浏览器限制**：localStorage 通常限制为 5-10MB
4. **路径格式**：使用绝对路径确保图片正确导入

## 更新日志

### v1.0.0
- ✅ 基础笔记编辑功能
- ✅ Markdown 导入/导出
- ✅ 图片自动处理和管理
- ✅ 设置配置系统
- ✅ 图片管理器
- ✅ 批量导入优化

## 笔记管理功能

### 删除单个笔记
1. 在编辑器工具栏点击**垃圾桶图标**删除当前笔记
2. 或在笔记管理器中选择要删除的笔记

### 批量管理笔记
1. 点击侧边栏的**"管理"**按钮打开笔记管理器
2. 功能包括：
   - **搜索笔记** - 按标题、分类或标签搜索
   - **批量选择** - 使用复选框选择多个笔记
   - **批量删除** - 删除选中的笔记
   - **清空所有** - 删除所有笔记（需要确认）

### 存储管理
- 查看存储空间使用情况
- 当存储空间不足时自动提示
- 支持智能清理旧笔记

### 安全提示
- 删除操作不可恢复
- 批量删除需要确认
- 清空所有需要输入 "DELETE ALL" 确认


