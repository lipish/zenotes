# Build frontend and deploy to Cloudflare Pages, and deploy Cloudflare Worker (api.zenotes.site)
#
# Repository Settings → Secrets → Actions configuration required:
#   CLOUDFLARE_API_TOKEN   Must have Workers and Pages write permissions
#   VITE_API_BASE          e.g., https://api.zenotes.site/api
# Optional:
#   PAGES_PROJECT_NAME     Default: zenotes (production domain zenotes.site)

name: Deploy Cloudflare Pages

on:
  push:
    branches: [main, feat/android-pwa]

jobs:
  worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: worker/package-lock.json
      - name: Install
        run: npm ci
      - name: Deploy Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [ -z "${CLOUDFLARE_API_TOKEN:-}" ]; then
            echo "::error::CLOUDFLARE_API_TOKEN is not set in repository Secrets"
            exit 1
          fi
          npx wrangler@4 deploy

  pages:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        env:
          VITE_API_BASE: ${{ secrets.VITE_API_BASE }}
        run: |
          if [ -z "${VITE_API_BASE:-}" ]; then
            echo "::error::VITE_API_BASE is not set in repository Secrets (e.g., https://api.zenotes.site/api)"
            exit 1
          fi
          npm run build

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          PAGES_PROJECT_NAME: ${{ secrets.PAGES_PROJECT_NAME }}
        run: |
          if [ -z "${CLOUDFLARE_API_TOKEN:-}" ]; then
            echo "::error::CLOUDFLARE_API_TOKEN is not set in repository Secrets"
            exit 1
          fi
          PROJECT="${PAGES_PROJECT_NAME:-zenotes}"
          npx wrangler@4 pages deploy dist --project-name="$PROJECT"